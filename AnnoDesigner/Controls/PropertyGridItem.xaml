<UserControl x:Class="AnnoDesigner.Controls.PropertyGridItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:AnnoDesigner.Controls">
    <Grid Margin="0,2,0,2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{Binding Header, RelativeSource={RelativeSource AncestorType=local:PropertyGridItem}}" />

        <ContentControl Grid.Column="1"
                        Content="{Binding DataContext, RelativeSource={RelativeSource AncestorType=local:PropertyGridItem}}"
                        ContentTemplate="{Binding ValueTemplate, RelativeSource={RelativeSource AncestorType=local:PropertyGridItem}}"
                        HorizontalAlignment="Right" />
    </Grid>
</UserControl>
