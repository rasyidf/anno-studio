<UserControl x:Class="AnnoDesigner.PropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnnoDesigner" xmlns:l="clr-namespace:AnnoDesigner.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Expander
            BorderBrush="{DynamicResource ExpanderHeaderBorderBrush}"
            DockPanel.Dock="Top"
            Header="{l:Localize LayoutSettings}"
            IsExpanded="True">
        <DockPanel LastChildFill="False">
            <Label Content="{l:Localize Version}"/>
            <TextBox
                    Width="140"
                    Height="32"
                    DockPanel.Dock="Right"
                    IsEnabled="True"
                    Text="{Binding LayoutSettingsViewModel.LayoutVersionDisplayValue}"/>
        </DockPanel>
    </Expander>
</UserControl>
